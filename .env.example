# Copy this file to .env and fill the values

# JWT secret for backend token signing
AUTH_SECRET=mysecretkey

# Bcrypt hash of your master password (preferred for production)
# Generate with Node locally:
#   node -e "console.log(require('bcryptjs').hashSync('your-password', 12))"
# Or use Dockerized Node:
#   docker run --rm node:20-alpine node -e "console.log(require('bcryptjs').hashSync('your-password', 12))"
AUTH_PASSWORD_HASH=$2b$12$aRdOBGYUSdUcM1pSPRRN3.2cU/g45x5/cSkRjdpIBOGbMTCfSiCTG
